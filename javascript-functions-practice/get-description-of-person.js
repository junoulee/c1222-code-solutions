/* exported getDescriptionOfPerson */
function getDescriptionOfPerson(person) {

  var a = person.name + ' ' + 'is a' + ' ' + person.occupation + ' ' + 'from' + ' ' + person.birthPlace + '.';
  return a;
}
