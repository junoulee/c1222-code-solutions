/* exported getFirstChar */
// run a for loop i=0 ; i < string.length; i++
// make it return string[0];

function getFirstChar(string) {
  for (var i = 0; i < string.length; i++);
  return string[0];
}
