/* exported getLastChar */
// run for loop i=0; i<string.length; i++
// make it return string[string.length-1]

function getLastChar(string) {
  for (var i = 0; i < string.length; i++);
  return string[string.length - 1];
}
